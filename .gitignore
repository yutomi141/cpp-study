# Mac固有のシステムファイル
.DS_Store

# コンパイル時に生成される実行ファイル (a.out)
a.out

# Macでのデバッグ用フォルダ
*.dSYM/

# 【プロの技】拡張子のない実行ファイルを一括で無視する
# 1. すべてのファイルを一旦無視の対象にする
*
# 2. ただし、フォルダは無視しない（中身を探索するため）
!*/
# 3. 拡張子（ドット）がついているファイル（.cpp, .mdなど）は無視しない
!*.*
# 4. .gitignore自体も無視しない
!.gitignore